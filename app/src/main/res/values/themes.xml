<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Vroomie_FE" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Vroomie_FE</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- 추가된 Material 전용 테마 -->
    <style name="Theme.Vroomie_FE.Material" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 필요 시 색상이나 폰트 커스터마이징 -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@android:color/black</item>
    </style>

</resources>